package model;

public class Theory {
    /*
    ---Theory advanced JPA Hibernate
    1.Relation
    *All relations annotations in JPA Hibernate
    + @OneToOne(name = "idSinhVien", referencedColumnName = "idSinhVien")
      Entity entity;
    + @ManyToMany(mappedBy ="Column_name")
        Collections<Entity> entity;
    +@OneToMany(mappedBy ="Column_name")
        Collection<Entity> entity;
    +@ManyToOne()
        Entity entity;
     +@JoinColumn(name"FK field",referencedColumnName = "PK filed");

     2.All Mapping Annotations
     @Entity
     @Table(name = "")
     @Column(name="")
     @Id
     @GeneratedValue(strategy="GenerationType.(IDENTITY,SEQUENCE,AUTO,TABLE));
     @Temporary(TemporaryTyped.(Date,Time,Timestamp));

     3.Query

     */
}
